kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.nfs_pvc.name  }}
spec:
  accessModes:
    {{ .Values.nfs_pvc.accessModes }}
  storageClassName: {{ .Values.nfs_pvc.storage_class }}
  resources:
    requests:
      storage:  {{ .Values.nfs_pvc.storage }}